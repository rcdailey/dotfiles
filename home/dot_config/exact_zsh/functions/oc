#!/usr/bin/env zsh
# Launch opencode with non-default settings.
# Route `oc upgrade` through mise, then refresh unpinned plugins.

if [[ "${1-}" == "upgrade" ]]; then
  mise upgrade npm:@opencode/cli "${@:2}" || return $?
  opencode plugin update
  return $?
fi

opencode "$@"
