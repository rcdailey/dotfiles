#!/usr/bin/env zsh
# Launch opencode with non-default settings.
# Route `oc upgrade` through mise so its postinstall hook refreshes the service and plugins.

if [[ "${1-}" == "upgrade" ]]; then
  mise upgrade npm:@opencode/cli "${@:2}"
  return $?
fi

opencode "$@"
