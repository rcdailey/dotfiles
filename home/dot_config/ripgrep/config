# Ripgrep configuration file
# This file is processed line-by-line as command-line arguments

# Search hidden files by default (dotfiles like .gitignore, .github/, etc.)
--hidden

# Exclude .git directory from searches (still ignores other VCS-ignored content)
--glob=!.git/*

# Follow symbolic links
--follow

# Smart case matching: case-insensitive unless pattern contains uppercase
--smart-case

# Show line numbers for context
--line-number

# Use heading format for better readability (groups matches by file)
--heading

# Truncate lines longer than 2000 characters (matches opencode grep tool behavior)
--max-columns=2000
--max-columns-preview
