# Homebrew environment setup moved to .zshenv for all shell sessions

# PATH modifications (moved from 05-configuration.sh for performance)
# Add user's private bin to PATH if it exists
if [ -d "$HOME/bin" ]; then
  PATH="$HOME/bin:$PATH"
fi

if [ -d "$HOME/.local/bin" ]; then
  PATH="$HOME/.local/bin:$PATH"
fi

# Go tools PATH moved to 05-configuration.sh for interactive shell access

# Append home dir to path so we can access scripts in there
export PATH="$PATH:$HOME:$HOME/git-scripts"
